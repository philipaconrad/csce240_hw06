download the 'assignment.zip' file from moodle

unzip that file to create a directory 'assignment'

run the script zzaFileCopyScript.txt to unzip and untar into 'testdirectory'

run the script zzbCompileScript.txt to compile all the programs

run the script zzcExecuteScript.txt to execute all the programs

execute 'more testdirectory/*/z*out*' to more through all the output

